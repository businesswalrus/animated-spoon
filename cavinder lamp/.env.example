# Field Service Calculator Environment Configuration
# Copy this file to .env and update with your values

# Application Environment (development, production)
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_NAME=field_service_calculator
DB_USER=your_db_user
DB_PASS=your_db_password

# Logging Configuration (for production)
LOG_DIR=/var/log/app

# Security Configuration
# Generate a strong key for session encryption (optional)
# SESSION_KEY=your-32-character-random-string

# Email Configuration (for future use)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_email_password
# SMTP_FROM=noreply@example.com

# Application Settings
# TIMEZONE=America/New_York
# LOCALE=en_US

# Rate Limiting Configuration
# MAX_LOGIN_ATTEMPTS=5
# LOCKOUT_MINUTES=15

# Session Configuration
# SESSION_TIMEOUT=3600
# SESSION_REGENERATE_TIME=1800